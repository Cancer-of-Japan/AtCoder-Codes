#include <iostream>
#include <time>
#include <math>
#include <random>


using namespace std

int main(){
    int a, b;
    cin >> a >> endl;
    cin >> b >> endl;
    int arr[a + b];
    if(a == 1 && b == 1){
        arr[0] = 1;
        arr[1] = -1;
    }
    
    
}
